.DS_Store
*.swp
*.swo
*~
.env
node_modules/
_password

# Claude Code local cache (per-user permissions, not shared)
.claude/settings.local.json

# npm pack output
*.tgz

# =========================
# Claude Code
# =========================
# The installed kit (.claude/) is a duplicate of templates/ — the real source.
# Don't track it in this repo.
.claude/
# Root Claude instructions — local only (templates/**/CLAUDE-*.md still ship).
/CLAUDE.md
